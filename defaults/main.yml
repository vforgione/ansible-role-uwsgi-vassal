---

python_version: 3.5

app_user: www-data

app_group: www-data

apps_dir: /www

source_dir: "{{ apps_dir }}/{{ app_name }}"

venvs_dir: /var/venvs

virtualenv_dir: "{{ venvs_dir }}/{{ app_name }}"

app_enabled: no

env_vars: []

uwsgi: {}

system_packages: []

python_packages: []
